/** World-level constants. The hand-authored island is gone — the map is now
 *  built from imported model assets placed with the in-game World Editor. */

/** Sea centre used by fish / monster patrol rings. */
export const SEA_CX = 0;
export const SEA_CZ = 44;

/** Character height reference (Angler feet y=0 → hat top ≈ 5.2). */
export const CHAR_H = 5.2;

/** Water surface level (mean). */
export const WATER_Y = 0;
